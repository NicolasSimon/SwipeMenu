<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    tools:context=".activity.SwipeExample">

    <view.SwipeMenu
        android:id="@+id/revealLayout"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        app:orientation="LeftToRight"
        app:minHeight="@dimen/reveal_layout_min_size_horizontal"
        app:maxHeight="@dimen/reveal_layout_max_size_horizontal"
        android:orientation="horizontal"
        android:gravity="left"
        android:layout_width="@dimen/reveal_layout_min_size_horizontal"
        android:layout_height="250dp">
        <TextView
            android:id="@+id/textField"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:background="@color/colorPrimaryDark"
            android:layout_width="@dimen/reveal_layout_min_size_horizontal"
            android:layout_height="match_parent"
            android:text="XX" />
        <ImageView
            android:layout_marginTop="5dp"
            android:layout_marginBottom="5dp"
            android:background="@mipmap/material_flat"
            android:scaleType="centerCrop"
            android:layout_width="@dimen/reveal_layout_diff_horizontal"
            android:layout_height="match_parent"/>
    </view.SwipeMenu>
</RelativeLayout>
